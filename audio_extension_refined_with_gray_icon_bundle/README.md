# Audio Extension Icons with Gray OFF Variant

`icons/` を Chrome 拡張のルート直下にコピーしてください。

カラー版: `icon16.png`, `icon32.png`, `icon48.png`, `icon128.png`
グレー版: `icon-gray16.png`, `icon-gray32.png`, `icon-gray48.png`, `icon-gray128.png`

OFF 状態にするときは JS 側で `chrome.action.setIcon()` を使ってグレー版に切り替えます。
